<h1><%= @visitor.name %>'s Meditation Friends</h1>

    <% @visitor.animals.each do |animal| %>
    <ul><%= animal.name  %> the
    <%= animal.species  %></ul>
    <% end %>
<%= button_to "Edit Visitor Name", edit_visitor_path,method: :get %>
