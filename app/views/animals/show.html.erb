<h1><%=@animal.name%> the <%=@animal.species%>'s friends</h1>

  <% @animal.visitors.each do |visitor| %>
  <ul><%= visitor.name %></ul>
  <% end %>

  <%= button_to "schedule a visit", new_forest_path(@forest), method: :get%>
