<h1><%=@animal.name%> the <%=@animal.species%>'s page</h1>

  <% @animal.visitors.each do |visitor| %>
  <%= visitor.name %>
  <% end %>

  <%= button_to "go to the forest", new_forest_path(@forest), method: :get%>
  
